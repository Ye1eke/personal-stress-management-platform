// Layout Components
export { Header } from './Header';
export { Navigation, defaultNavigationItems } from './Navigation';
export { Footer } from './Footer';
export {
  Breadcrumb,
  generateBreadcrumbs,
  defaultPathLabels,
} from './Breadcrumb';

// Re-export types if needed
// Note: Types are defined inline in the components
